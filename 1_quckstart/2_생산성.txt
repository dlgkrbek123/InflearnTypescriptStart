------------------------------------------------------------

// 1.js

const mike = { friends: ['june', 'james'] }
const totalFriends = mike.friendList.lenngth

// 속성이름이 잘못되어 잘못된 속성을 입력해도 IDE가 에러를 발견 x

------------------------------------------------------------

// 1.ts

const mike = { friends: ['june', 'james'] }
const totalFriends = mike.friendList.lenngth

// mike의 타입을 알 수 있어서 IDE가 에러를 발견해준다.
// 또한 mike.으로 접근시 접근가능한 key에 대한 힌트가 알맞게 제공된다.

------------------------------------------------------------

에러를 미리발견할 가능성이 높고
변수의 타입을 미리 알아 IDE에서 힌트를 알맞게 제공한다.

------------------------------------------------------------
